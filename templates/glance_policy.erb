{
  "context_is_admin":  "role:admin",
  "default": "",
<% if @allow_upload_img_admin_only == true %>
  "upload_image": "role:admin",
<% else %>
  "upload_image": "",
<% end %>
<% if @allow_copy_img_admin_only == true %>
  "copy_from": "role:admin",
<% else %>
  "copy_from": "",
<% end %>
<% if @allow_publicize_img_admin_only == true%>
  "publicize_image": "role:admin",
<% else %>
  "publicize_image": "",
<% end %>
  "manage_image_cache": "role:admin"
}
